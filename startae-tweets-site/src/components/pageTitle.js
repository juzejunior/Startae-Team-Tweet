import React from "react"

const PageTitle = ({ title }) => (
  <span
    style={{
      color: "white",
      fontSize: "4em",
      fontFamily: "ProximaNova",
      fontWeight: "bold",
      marginBottom: 8,
      lineHeight: 1,
    }}
  >
    {title}
  </span>
)

export default PageTitle
