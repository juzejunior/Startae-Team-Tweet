import React from "react"

const PageTitle = ({ title }) => (
  <span
    style={{
      color: "white",
      fontSize: "4em",
      marginBottom: 8,
      lineHeight: 1,
      fontFamily: "Proxima Nova Bold",
    }}
  >
    {title}
  </span>
)

export default PageTitle
