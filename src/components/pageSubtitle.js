import React from "react"

const PageSubTitle = ({ subtitle }) => (
  <p style={{ color: "white", fontSize: 16, maxWidth: "35em", marginTop: 16 }}>
    {subtitle}
  </p>
)

export default PageSubTitle
